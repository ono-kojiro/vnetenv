- name: setup bridge and network
  hosts: myserver
  
  #environment:
  #  http_proxy: http://proxy.example.com:8080
  #  https_proxy: http://proxy.example.com:8080

  roles:
  - name: bridge
    tags: bridge

  - name: network
    tags: network

